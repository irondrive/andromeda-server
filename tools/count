#!/bin/bash

rm combined.php

getfiles() {
    find $1 -name '*.php' | grep -v "/data/\|/docs/\|/tools/\|/vendor/"
}

for i in . apps/* core; do echo $i; echo $(getfiles $i | wc -l) files; getfiles $i | xargs -d '\n' wc -l | sort -r; done
